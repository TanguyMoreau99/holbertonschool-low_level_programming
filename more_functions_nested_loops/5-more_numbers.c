#include "main.h"

/**
 * more_numbers - check
 *
 * Return: Always 0.
 */

void more_numbers(void)
{
	int t;
	int x;

	for (t = 1; t <= 10; t++)

		for (x = 0; x <= 14; x++)
			_putchar(x);

	_putchar(10);
}
