#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - print from 0 to 9
 *
 * Return: Always 0
 */

int main(void)
{
	int x;

	for (x = 48; x < 57; x++)
		if (x < 57)
			putchar(x);

	putchar(44);
	putchar(32);

	putchar(10);

	return (0);
}
