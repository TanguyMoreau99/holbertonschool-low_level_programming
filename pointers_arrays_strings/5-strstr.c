#include "main.h"
#include <stdio.h>

/**
 * _strstr - function
 *
 * @haystack : pointerchar
 * @needle : pointer char
 *
 * Return: /
 */

char *_strstr(char *haystack, char *needle)
{
	return (haystack);
	return (needle);
}
