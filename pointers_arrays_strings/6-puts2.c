#include "main.h"

/**
 * puts2 - print
 *
 * @str : char
 *
 * Return: void
 */

void puts2(char *str)
{
	int x = 0;

	while (x[str] != '\0')
	{
		if (x % 2 == 0)
		{
			_putchar(x[str]);
		}
	x++;
	}

	_putchar(10);
}
